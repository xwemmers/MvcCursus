@{
    // Deze code wordt voor elke View standaard uitgevoerd

    // Deze code wordt NIET uitgevoerd voor een PartialView

    // Gebruikelijk is om in deze file alleen C# code te plaatsen

    Layout = "_Layout";
}
